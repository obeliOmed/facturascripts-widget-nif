{
  "name": "wnif-e2e",
  "private": true,
  "description": "WidgetNif — Playwright E2E tests. Playwright is provided by the /GitHub workspace root; run `npx playwright test` from this folder (Node ancestor resolution finds the root binary).",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  }
}
